// Package finding holds the Finding domain model (AGENTS.md §7).
//
// A Finding is the fact that a given Vulnerability was detected on a given
// Asset. It must never be conflated with the Vulnerability itself
// (Invariant 1, §44).
package finding
